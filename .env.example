# PostgreSQL connection string
# Configure DATABASE_URL in Render or your local environment.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Set to production on Render so PostgreSQL SSL is enabled.
NODE_ENV=development
